_schema-version: "3.2"
ID: comkposmeddapplication
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comkposmeddapplication-destination-content
  type: com.sap.application.content
  requires:
  - name: comkposmeddapplication-destination-service
    parameters:
      content-target: true
  - name: comkposmeddapplication-repo-host
    parameters:
      service-key:
        name: comkposmeddapplication-repo-host-key
  - name: comkposmeddapplication-uaa
    parameters:
      service-key:
        name: comkposmeddapplication-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comkposmeddapplication_html_repo_host
          ServiceInstanceName: comkposmeddapplication-html5-srv
          ServiceKeyName: comkposmeddapplication-repo-host-key
          sap.cloud.service: comkposmeddapplication
        - Authentication: OAuth2UserTokenExchange
          Name: comkposmeddapplication_uaa
          ServiceInstanceName: comkposmeddapplication-xsuaa-srv
          ServiceKeyName: comkposmeddapplication-uaa-key
          sap.cloud.service: comkposmeddapplication
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comkposmeddapplication-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comkposmeddapplication-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comkposmeddapplication.zip
      name: comkposmeddapplication
      target-path: resources/
- name: comkposmeddapplication
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comkposmeddapplication-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comkposmeddapplication-destination-service
    service-plan: lite
- name: comkposmeddapplication-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comkposmeddapplication-xsuaa-srv
    service-plan: application
- name: comkposmeddapplication-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comkposmeddapplication-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
